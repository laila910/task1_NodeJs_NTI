{{>header}}
{{>navbar}}

<section class="container my-3">
    <form method="post">
        <div class="row">
            <div class="mb-3 col-6">
                <label class="label-control mb-2">Add Balance Amount</label>
                <input type="number" placeholder="amount" class="form-control" name="amount">
            </div>

            <div class="mb-3 col-12">
                <button class="btn btn-primary">add Balance</button>
            </div>

        </div>
    </form>
</section>

{{>footer}}
